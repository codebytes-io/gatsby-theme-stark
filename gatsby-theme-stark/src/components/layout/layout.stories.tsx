import React from "react";
import Layout from "./layout";

export default {
  title: "Layout",
  component: Layout,
};

export const Default = () => (
  <Layout>
    <p>Content here</p>
  </Layout>
);
