/** @jsx jsx */
import { jsx } from "theme-ui";

const Button = () => {
  return (
    <button
      sx={{
        padding: 2,
        color: "background",
        backgroundColor: "primary",
      }}
    >
      A button
    </button>
  );
};

export default Button;
